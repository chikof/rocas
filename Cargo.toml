[workspace]
resolver = "2"
members = ["rocas", "watcher"]

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
panic = "abort"

[workspace.dependencies]
log = { version = "0.4.29", features = ["std"] }
